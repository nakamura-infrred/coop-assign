rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TODO: tighten rules based on tenantId and user roles.
      allow read, write: if false;
    }
  }
}
